language: node_js  #设置语言
node_js: v8.1.3  
install:
  - npm install  

before_install:
  - npm install -g hexo-cli

script:
  - hexo cl 
  - git submodule init
  - git submodule update 
  - hexo g  

after_success:
  - cd ./public
  - git init
  - git config user.name "tripleCC" 
  - git config user.email "triplec.linux@gmail"
  - git add .
  - git commit -m "update"
  - git push --force --quiet "https://${Token}@${GH_REF}" master:master 

branches:
  only:
    - hexo  
env:
 global:
   - GH_REF: github.com/tripleCC/tripleCC.github.io.git  #设置GH_REF，注意更改yourname